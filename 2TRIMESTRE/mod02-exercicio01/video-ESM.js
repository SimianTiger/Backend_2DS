export function alugarFilme(nomedoFilme, dataAluguel, preço) {
  return `O filme ${nomedoFilme} tem 30 dias para ser devolvido`
}

export function devolverFilme(nomedoFilme, dataDevolução) {
  return `O filme ${nomedoFilme} precisa ser entregado até hoje`
}